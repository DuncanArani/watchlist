from app import app 


if __name__ == 'main':
    app.run()
    # use the app.run() method to launch our server. We pass in the debug = Trueargument to allow us to run on debug mode so that we can easily track errors in our application.